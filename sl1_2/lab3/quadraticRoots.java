/*
Author: Kevin Qiu
Class: IB Computer Science 1-2 Period 3
Last Edited: Aug. 29, 2018
*/

import java.util.Scanner;

class quadraticRoots {
    public static void main(String Args[]) {

        /*
         Ax^2 + Bx + C = 0
         coeff1 = A
         coeff2 = B
         const = C
        */
        double coeff1 = 0;
        double coeff2 = 0;
        double constant = 0;
        double root1 = ((-coeff2 + Math.sqrt(Math.pow(coeff2, 2) - (4 * coeff1 * constant))) / (2 * coeff1));
        double root2 = ((-coeff2 - Math.sqrt(Math.pow(coeff2, 2) - (4 * coeff1 * constant))) / (2 * coeff1));

        Scanner input = new Scanner(System.in);
        System.out.println("Please")


    }
}
